<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="SPOG.Views.AboutPage">
    <ContentPage.Content>
        <StackLayout VerticalOptions="Center" HeightRequest="300">
            <Grid x:Name="Content" RowSpacing="10" >
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Label Grid.Row="0" Text="{Binding AppName}"
 
                HorizontalOptions="CenterAndExpand" />
                <Label Grid.Row="1" Text="{Binding AppBuild}"
              
                HorizontalOptions="CenterAndExpand" />
                <Label Grid.Row="2" Text="{Binding PackageName}"
               
                HorizontalOptions="CenterAndExpand" />
                <Label Grid.Row="3" Text="{Binding AppVersion}"
             
                HorizontalOptions="CenterAndExpand" />
            </Grid>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>